<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Diary Dashboard</title>
  <link rel="stylesheet" href="styles.css">
</head>
<body>
  <div class="dashboard">
    <nav class="navbar">
      <div class="logo">My Diary</div>
      <ul class="menu">
        <li><a href="#">Home</a></li>
        <li><a href="#">All Entries</a></li>
        <li><a href="#">Achievements</a></li>
        <li><a href="/logout">Logout</a></li>
      </ul>
      <% if(user==null){ %>
        <div class="profile">👤 Profile</div>
      <% }else { %>
        <div class="profile">👤 <%= user %></div>
      <% } %>
    </nav>

    <div class="content">
      <aside class="sidebar">
        <h3>Calendar</h3>
        <div class="calendar-navigation">
          <button id="prev-month">&lt;</button>
          <span id="current-month">November 2024</span>
          <button id="next-month">&gt;</button>
        </div>
        <div id="calendar-grid"></div>
      </aside>

      <main class="main-section">
        <div class="top-bar">
          <h2 id="selected-date">Selected Date: <span id="show-date">loading...</span></h2>
          <h2 id="notify"></h2>
        </div>
        <div id="entry-section">
          <textarea id="diary-entry" placeholder="Select a date from calendar..." style="width:100%; height:200px;"></textarea>
          <button id="save-button" class="save-btn">Save</button>
        </div>
      </main>
    </div>
  </div>
  <script src="script.js"></script>
</body>
</html>
